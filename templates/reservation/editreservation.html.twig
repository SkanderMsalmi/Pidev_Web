{% extends 'base.html.twig' %}


{% block title %}Modifier Reservation{% endblock %}

{% block body %}
    <div class="row">
        <div  style="display: flex;align-items: center;justify-content: center;margin-top: 150px; padding: 10px;">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Modifier une reservation</h5>

                    <!-- General Form Elements -->
                    {{ form_start(ff,{attr:{novalidate:'novalidate'}}) }}
                    <label for="inputText" class="col-sm-5 col-form-label">nom</label>
                    <div class="col-sm-10">
                        {{ form_widget(ff.nom,{'attr':{'class':'form-control'}}) }}

                        {{ form_errors(ff.nom,{'attr':{'class':'form-danger'}}) }}

                    </div>




                    <br/>
                    <div class="col-sm-10" style="display: flex;align-items: center;justify-content: center">

                        {{ form_widget(ff.Ajouter,{'attr':{'class':'btn btn-primary'}}) }}
                    </div>

                    {{ form_end(ff) }}

                    <a href="{{ 'reservation' }}" class="text-body"><i class="fas fa-long-arrow-alt-left me-2"></i>Retour à la liste des reservations</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
