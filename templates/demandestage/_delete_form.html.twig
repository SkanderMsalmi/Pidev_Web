<form method="post" action="{{ path('app_demandestage_delete', {'iddemande': demandestage.iddemande}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demandestage.iddemande) }}">
    <button class="btn">Delete</button>
</form>
