{% extends 'base.html.twig' %}

{% block title %}Demandestage{% endblock %}

{% block body %}
    <h1 style="padding:50px">Demande stage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Iddemande</th>
                <td>{{ demandestage.iddemande }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ demandestage.etat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_demandestage_index') }}">back to list</a>

    <a href="{{ path('app_demandestage_edit', {'iddemande': demandestage.iddemande}) }}">edit</a>

    {{ include('demandestage/_delete_form.html.twig') }}
{% endblock %}
