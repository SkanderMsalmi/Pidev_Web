{% extends 'baseAdmin.html.twig' %}

{% block title %}Question{% endblock %}

{% block body %}
<div style="display: flex;align-items: center;justify-content: center;padding:150px">
   <div>
       <h5 class="card-title">Question</h5>
        <div style="display: flex;align-items: right;justify-content: right;">
        </div>
        <table class="table">
    <table class="table">
        <tbody>
            <tr>
                <th>Idquestion</th>
                <td>{{ question.idquestion }}</td>
            </tr>
            <tr>
                <th>Enonce</th>
                <td>{{ question.enonce }}</td>
            </tr>
            <tr>
                <th>Domaine</th>
                <td>{{ question.domaine }}</td>
            </tr>
            <tr>
                <th>Reponsecorrecte</th>
                <td>{{ question.reponsecorrecte }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-primary" href="{{ path('afficherQuestions') }}">back to list</a>

    <a class="btn btn-outline-secondary" href="{{ path('modifierQuestion', {'idquestion': question.idquestion}) }}">edit</a>

    {{ include('question/_delete_form.html.twig') }}
{% endblock %}
