{% extends 'base.html.twig' %}

{% block title %}Proposition{% endblock %}

{% block body %}
    <h1>Proposition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idproposition</th>
                <td>{{ proposition.idproposition }}</td>
            </tr>
            <tr>
                <th>Proposition</th>
                <td>{{ proposition.proposition }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_proposition_index') }}">back to list</a>

    <a href="{{ path('app_proposition_edit', {'idproposition': proposition.idproposition}) }}">edit</a>

    {{ include('proposition/_delete_form.html.twig') }}
{% endblock %}
