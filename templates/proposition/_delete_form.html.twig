<form method="post" action="{{ path('app_proposition_delete', {'idproposition': proposition.idproposition}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ proposition.idproposition) }}">
    <button class="btn">Delete</button>
</form>
